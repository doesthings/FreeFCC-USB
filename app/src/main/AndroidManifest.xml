<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- USB accessory mode (TOP port): the RC-N1/RC-N2/RC-N3 is the USB host,
         the phone is the accessory. DJI controllers use the Android Open
         Accessory (AOA) protocol for the DJI Fly pipe. -->
    <uses-feature android:name="android.hardware.usb.accessory" android:required="false" />

    <!-- USB host mode (BOTTOM port): the RC-N1/RC-N2/RC-N3 bottom (service)
         port presents as a CDC ACM serial device (VID 0x2CA3 / PID 0x1020).
         This is where DUMPL commands are most reliably accepted. -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Query DJI flight apps so we can relaunch them after enabling FCC. -->
    <queries>
        <package android:name="dji.go.v5" />
        <package android:name="dji.go.v4" />
        <package android:name="dji.pilot" />
        <package android:name="com.dji.industry.pilot" />
        <package android:name="com.dji.industry.pilot.pe" />
        <package android:name="dji.prof.args.pad" />
        <package android:name="com.dji.vision" />
        <package android:name="dji.pilot.pad" />
    </queries>

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.FreeFCCN1"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Launch the app automatically when a DJI RC is plugged in via USB -->
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED" />
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_ACCESSORY_ATTACHED"
                android:resource="@xml/accessory_filter" />
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

    </application>
</manifest>